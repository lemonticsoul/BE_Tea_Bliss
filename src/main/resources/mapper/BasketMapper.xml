<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="store.teabliss.basket.mapper.BasketMapper">

    <insert id="save">
        INSERT INTO Basket(img,productId,name,nameEng,price,quality,type)
        VALUES (#{img},#{productId},#{name},#{nameEng},#{price},#{quality},#{type})
    </insert>


    <select id="getbasket">
        select *
        from Basket
    </select>

    <update id="update">
        UPDATE Basket
        SET quality= IFNULL(#{quality}, quality),
        type = IFNULL(#{type}, type)
        WHERE productId=#{productId}

    </update>

    <delete id="delete">
        DELETE FROM Basket
        WHERE 1=1
        AND productid= #{prodcutid}
    </delete>

</mapper>